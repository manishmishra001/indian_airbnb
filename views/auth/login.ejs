<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/output.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  </head>
  <body class="bg-gray-800 min-h-screen flex items-center justify-center pt-20">
    <main class="bg-gray-900 p-8 rounded-xl shadow-2xl max-w-sm w-full flex justify-center items-center border border-gray-700">
      <form action="/login" method="POST" class="space-y-6 w-full">
        <h2 class="text-3xl font-bold text-white text-center mb-6 font-inter">Welcome Back</h2>
        

        <% if (errors.length > 0) { %> 
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">      
          <ul class="list-disc list-inside space-y-1">
            <% errors.forEach(error => { %>
              <li><%= error %></li>
            <% }); %>
          </ul>
         </div>        
      <% } %>
    
   
        <input
          type="email"
          name="email"
          value="<%= oldInput.email ? oldInput.email : '' %>"
          placeholder="Enter your Email"
          class="w-full px-4 py-3 bg-gray-700 text-white border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600 placeholder-gray-400" required
        />
        
        <input
          type="password"
          name="password"
          placeholder="Enter your Password"
          class="w-full px-4 py-3 bg-gray-700 text-white border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-purple-600 placeholder-gray-400"
          required
        />

        <button
          type="submit"
          class="w-full bg-purple-600 text-white py-3 rounded-lg font-bold hover:bg-purple-700 transition duration-300"
        >
          Login
        </button>
      </form>
    </main>
  </body>
</html>