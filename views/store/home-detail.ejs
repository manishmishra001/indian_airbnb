<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-800 min-h-screen ">
    <%- include('../partials/nav') %>
    <main class="container mx-auto  p-4 md:p-6 font-inter">
        <div class="bg-gray-900 shadow-xl rounded-lg overflow-hidden  md: pr-2 pl-2   ml-1 mr-1 border border-gray-700">
            <h2 class="text-3xl sm:text-4xl text-white font-bold text-center mb-6 p-3 md:mb-6">
                <%= home.houseName %>
            </h2>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8">
                <div class="rounded-lg overflow-hidden">
                    <img src="<%= home.photo %>" alt="<%= home.name %>" class="w-full h-auto md:h-96 object-cover rounded-lg">
                    <div class="md:pl-6 lg:pl-12 mt-7">
                        <a href="/rules/<%= home.id %>" class="inline-block mb-4 text-purple-400 hover:text-purple-600 font-semibold transition duration-300">

                        </a>
                    </div>
                </div>
            
                <div class="space-y-6">
                    <!-- Description with scroll -->
                    <div class="pb-4 border-b border-gray-700">
                        <h3 class="text-xl sm:text-2xl font-semibold text-white mb-2">Description</h3>
                        <p class="text-gray-400 leading-relaxed max-h-40 overflow-y-auto pr-2">
                            <%= home.description %>
                        </p>
                    </div>

                    <!-- Location -->
                    <div class="pb-4 border-b border-gray-700">
                        <h3 class="text-xl sm:text-2xl font-semibold text-white mb-2">Location</h3>
                        <p class="text-gray-400 flex items-center">
                            <i class="fas fa-map-marker-alt mr-2 text-purple-400"></i>
                            <%= home.location %>
                        </p>
                    </div>
                    
                    <!-- Price -->
                    <div class="pb-4 border-b border-gray-700">
                        <h3 class="text-xl sm:text-2xl font-semibold text-white mb-2">Price</h3>
                        <p class="text-purple-400 text-2xl sm:text-3xl font-bold">Rs<%= home.price %> / night</p>
                    </div>
                    
                    <!-- Rating -->
                    <div class="pb-4 border-b border-gray-700">
                        <h3 class="text-xl sm:text-2xl font-semibold text-white mb-2">Rating</h3>
                        <div class="flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" class="w-5 h-5 sm:w-6 sm:h-6 text-yellow-400 mr-2" viewBox="0 0 24 24">
                                <path fill-rule="evenodd" d="M10.788 3.21c..."></path>
                            </svg>
                            <span class="ml-1 text-xl sm:text-2xl font-semibold text-gray-300"><%= home.rating %> / 5</span>
                        </div>
                    </div>

                    <!-- Favourite button -->
                    <div class="flex justify-center md:justify-start">
                        <div class="bg-green-300 p-2 hover:bg-green-500 w-full sm:w-1/2 rounded-lg flex justify-center items-center border border-gray-700 hover:border-purple-600 transition duration-300 cursor-pointer my-6 md:my-4">
                            <%- include('../partials/favourite') %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>
</html>